Твоя задача: выполнить Tasks 1-4 (Wave 1 — Backend) плана groq-sentences-obsidian.

План доступен в .omo/plans/groq-sentences-obsidian.md.

Конкретные файлы и изменения:

Task 1 — config.py + requirements.txt:
- backend/app/config.py: добавь в класс Settings поля: groq_api_key: str и groq_model: str = "llama-3.3-70b-versatile"
- backend/requirements.txt: добавь строку "groq>=1.0.0"
- НЕ пытайся читать .env — GROQ_API_KEY уже там

Task 2 — groq_service.py:
- Создай backend/app/services/groq_service.py
- Singleton GroqService (паттерн как в GeminiService)
- Метод: generate_sentence(word, language, front_meaning, back_meaning) -> dict с ключами sentence_in_target и translation_in_russian
- Используй from groq import Groq
- model=settings.groq_model, temperature=0.7, max_tokens=150

Task 3 — groq router:
- Создай backend/app/routers/groq.py
- POST /api/groq/sentence endpoint с телом {language: "en"|"sk"}
- Логика: получить due-карточки (Card, не CardReverse) через db.query, выбрать случайную, вызвать groq_service.generate_sentence, вернуть JSON

Task 4 — main.py:
- backend/app/main.py: добавь import groq и app.include_router(groq.router, prefix="/api/groq", tags=["groq"])

После всех изменений запусти: cd backend && pip install groq && python -c "from app.main import app; print('OK')"

Файлы НЕ трогать: frontend/** ничего, backend/app/services/gemini_service.py, backend/app/routers/cards.py, backend/app/routers/obsidian.py
